# The run the recorded score was taken from

`summary.json`, `runs.json` and `run-manifest.json` of `run_fc08423b4a78bd03`: the thirty-fixture
regression suite at one repeat on 2026-09-11, reviewer `reviewer_v9` on `claude-opus-5` over
`claude-cli`, routing `d065`. It is the run `.github/regression-score.json` records.
The files are here so the score's provenance travels with it: anyone holding this tree can
re-derive what the score says from what the harness wrote, without a model call. The harness is
the only thing that scores a run; nothing here re-scores it.
