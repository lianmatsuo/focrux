/** Artwork has semantic names in InkIcon. Navigation arrows are used only for pagination.
 * The dropdown in Screen is copied from the original mockup's inline SVG.
 * There are deliberately no generic icon aliases: play, download and external links
 * must not silently reuse the next-page artwork.
 */
export {};
